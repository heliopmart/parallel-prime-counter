#include "matrix.h"
#include <iostream>

int _calculateAndPrintTime(std::chrono::steady_clock::time_point start, std::chrono::steady_clock::time_point end, const std::string& task_name);

bool isPrime(int n);